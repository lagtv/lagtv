.row
  .span9
    %h1 Reset your password

    = simple_form_for @user, :url => password_reset_path(params[:id]), :validate => true, :html => { :class => 'form-horizontal' } do |f|
      = f.input :password
      = f.input :password_confirmation

      .form-actions
        = f.button :submit, :value => "Update Password", :class => "btn btn-large btn-primary"
        = link_to "Cancel", root_path, :class => "btn btn-large"

  .span3
    %p Please enter a new password for your account and it will be reset and allow you to login again.