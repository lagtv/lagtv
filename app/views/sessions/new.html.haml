.row
  .span9
    %h1 Login

    = simple_form_for :session, :url => sessions_path, :html => { :class => 'form-horizontal' } do |f|
      = f.input :email
      = f.input :password, :input_html => { :autocomplete => 'off' }
      = f.input :remember_me, :as => :boolean
      
      .form-actions
        = f.button :submit, :value => "Login", :class => "btn btn-large btn-primary"
        = link_to "Cancel", root_path, :class => "btn btn-large"

    = link_to "Forgotten your password?", new_password_reset_path

  .span3
    %p You need an account before you can upload replay files or post in the forums.
    %p 
      If you do not have an account, you can 
      = link_to "sign up for one.", register_path 
  
  
